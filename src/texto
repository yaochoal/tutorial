
comando para instalar 
npm install --save redux
creamos el store

///////////////////
import { createStore } from 'redux';

const reducer = (state,action) => {
    return state;
}

export default createStore(reducer);

////////////////////////
Agregamos el store.dispatch a un método de ejecución
/////////////////////////////
store.dispatch({
         
     })
